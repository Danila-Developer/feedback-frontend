import React from 'react'

import classes from './Button.module.css'

export default function Button({text, wrapperStyle, ...props}) {
    return (
        <div style={wrapperStyle}>
            <div className={classes.button} {...props}>
                {text}
            </div>
        </div>
    )
}