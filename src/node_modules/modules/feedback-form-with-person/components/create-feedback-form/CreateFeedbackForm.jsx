import React, {useState} from 'react';
import classes from 'modules/feedback-form-with-person/components/create-feedback-form/CreateFeedbackForm.module.css'
import MultipleFileUpload from 'components/multiple-file-upload/MultipleFileUpload'

const CreateFeedbackForm = ({errors, register}) => {
    const [uploadedFiles, setUploadedFiles] = useState([])

    return (
        <div className='container'>
            <div className={classes.feedbackFormWrapper}>
                <h2 className={classes.feedbackFormTitle}>Отзыв на специалиста</h2>
                <div className={classes.feedbackForm}>
                    <MultipleFileUpload setUploadedFiles={setUploadedFiles} uploadedFiles={uploadedFiles} />
                </div>
            </div>
        </div>

    );
};

export default CreateFeedbackForm;